##Write a function rectangle_area(length, width) that returns the area of a rectangle.
##Test it with length = 4, width = 5.

rectangle_area <- function(length, width) {
  area <- length * width
  return(area)
}


rectangle_area(length = 4, width = 5)